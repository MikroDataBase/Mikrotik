{
  "name": "my-v0-project",
  "version": "0.1.0",
  "private": false,
  "scripts": {
    "build": "echo 'no build script'"
     "scripts": {
    "build": "[my-framework] build --output public"
  }
}
